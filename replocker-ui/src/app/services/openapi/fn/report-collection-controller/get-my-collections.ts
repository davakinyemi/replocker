/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseReportCollectionResponse } from '../../models/page-response-report-collection-response';

export interface GetMyCollections$Params {
  page?: number;
  size?: number;
}

export function getMyCollections(http: HttpClient, rootUrl: string, params?: GetMyCollections$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseReportCollectionResponse>> {
  const rb = new RequestBuilder(rootUrl, getMyCollections.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseReportCollectionResponse>;
    })
  );
}

getMyCollections.PATH = '/report-collections/my';
